{
  "id": "disposable-password",
  "type": "object",
  "required": ["id", "challengeType"],
  "properties": {
    "challengeType": {
      "default": "phone",
      "enum": ["phone"],
      "$ref": "common.json#/definitions/challengeType"
    },
    "id": {
      "type": "string",
      "description": "User identificator"
    }
  }
}
